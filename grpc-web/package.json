{
  "name": "grpc-web",
  "version": "1.0.0",
  "description": "API proxy that demonstrates a gRPC-web passthrough proxy",
  "scripts": {
    "lint": "apigeelint -s bundle/apiproxy -e PO013,PO025,TD002,TD004 -f table.js",
    "test": "npx cucumber-js --publish-quiet test/"
  },
  "dependencies": {
    "@cucumber/cucumber": "^7.3.0",
    "apickli": "^3.0.1",
    "apigeelint": "latest"
  }
}
